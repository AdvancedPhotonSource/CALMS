7
100 Recursive chunking divides the input text into smaller chunks in a hierarchical and iterative manner
96 iterative manner using a set of separators. If the initial attempt at splitting the text doesn’t
94 the text doesn’t produce chunks of the desired size or structure, the method recursively calls
98 recursively calls itself on the resulting chunks with a different separator or criterion until the
97 criterion until the desired chunk size or structure is achieved. This means that while the chunks
92 while the chunks aren’t going to be exactly the same size, they’ll still “aspire” to be of a
33 “aspire” to be of a similar size.
